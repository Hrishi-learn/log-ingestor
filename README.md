# log-ingestor
Log-Ingestor is a microservice that accepts structured log data from various applications via a REST API, validates the input, and publishes the logs to a Kafka topic for downstream processing and analysis.
